# ここへ音源を入れてください

このフォルダーにWAV / MP3などの音源を追加します。下位フォルダーも使えます。

GitHub：**Add file → Upload files** からアップロード。

PC：コピー後、リポジトリ直下で `npm run samples:index` を実行し、生成された `dist/samples/catalog.json` もcommit / push。

このREADMEは音源一覧に登録されません。ここに置いた音源は公開されます。
